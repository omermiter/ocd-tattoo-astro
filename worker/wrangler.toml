name = "ocd-tattoo-contact"
main = "src/index.ts"
compatibility_date = "2025-01-01"
workers_dev = true

# TODO(client): set to the real site origin once the custom domain is live
# (e.g. "https://ocdtattoo.com"). Keeping this specific (not "*") stops other
# sites from using your Resend relay/API key.
[vars]
# Temporarily localhost for local testing — MUST be changed to the real
# production origin (e.g. "https://ocdtattoo.com") before/when going live,
# then redeploy with `npm run deploy`.
ALLOWED_ORIGIN = "http://localhost:4321"
